{
  "name": "pangram-gallery",
  "version": "0.1.4",
  "private": true,
  "description": "Replace Pangram-labeled AI feed posts with quiet public-domain art cards.",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-extension.mjs && node --check src/content-core.js && node --check src/content.js && node --check src/options/options.js && node --check src/background/service-worker.mjs && node --check src/background/providers.mjs && node --test"
  }
}
